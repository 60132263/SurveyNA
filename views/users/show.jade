extends ../layout

block content
  .container
    h1.page-header 회원 정보
          
    ul
      li 이름: #{user.name}
      li 이메일: #{user.email}
      if user.pos===1 
        li 권한: 회원
      if user.pos===0 
        li 권한: 관리자
      if user.facebook
        li Facebook 
          img.img-circle(src='#{user.facebook.photo}')

    p
      if user.pos===0
        a.btn.btn-default(href='/users/') 관리
        | 
      a.btn.btn-default(href='/users/#{user._id}/edit') 수정
      | 
      a.btn.btn-default(href='/users/#{user._id}?_method=DELETE') 탈퇴
